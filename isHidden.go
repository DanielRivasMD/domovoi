////////////////////////////////////////////////////////////////////////////////////////////////////

package domovoi

////////////////////////////////////////////////////////////////////////////////////////////////////

import (
	"strings"
)

////////////////////////////////////////////////////////////////////////////////////////////////////

// isHiddenFile returns true if the given filename starts with a dot,
// which is the common convention for hidden files on Unix-like systems.
func isHiddenFile(filename string) (bool, error) {
	return strings.HasPrefix(filename, "."), nil
}

////////////////////////////////////////////////////////////////////////////////////////////////////
